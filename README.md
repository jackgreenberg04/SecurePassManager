# SecurePassManager

A lightweight command-line password manager written in Java. It allows you to store, retrieve, delete, and manage credentials for different websites. Data is persisted using Java serialization.

To compile all sources:

```bash
javac -d bin src/*.java
```

To run the application:

```bash
java -cp bin securepassmanager.SecurePassManager
```
